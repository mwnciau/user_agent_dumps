# Crawler User Agents

See [monperrus/crawler-user-agents](https://github.com/monperrus/crawler-user-agents).

> A list of of HTTP user-agents used by robots, crawlers, and spiders as in single JSON file.

The list of crawler user agents is compiled into a single list of parsed user agents.
